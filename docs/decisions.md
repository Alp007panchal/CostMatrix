# Decisions log

One line per decision. Never delete a line; if a decision is reversed, add a new line that
says so and references the old number.

| # | Date | Decision |
|---|---|---|
| D-001 | 2026-09-04 | Labour is fixed hours × hourly rate per process type, never a percentage of material. |
| D-002 | 2026-09-04 | Three process types: panel/component assembly, wiring, busbar fabrication and assembly. Each has its own hourly rate. |
| D-003 | 2026-09-04 | Kit and standard assembly are one concept, one level; called "assembly" in the app, "kit" mentioned as a synonym. |
| D-004 | 2026-09-04 | KES is the master currency; all master prices and master default rates are in KES. |
| D-005 | 2026-09-04 | A company may work in another currency. Company admin sets currency and exchange rate. |
| D-006 | 2026-09-04 | Exchange rate is stored as KES per 1 unit of company currency, and is frozen into each costing. |
| D-007 | 2026-09-04 | Master labour hours are visible to all companies; a company admin may override them per assembly at company level. |
| D-008 | 2026-09-04 | Labour hours of a private assembly are set by the company admin. |
| D-009 | 2026-09-04 | In-house company uses the same discount mechanism with 0 %; there is no separate cost price. |
| D-010 | 2026-09-04 | A costing contains several panels, each with a quantity, each with its own assemblies. |
| D-011 | 2026-09-04 | Master admin has read-only access to all company data and full control of company settings, users and discounts. |
| D-012 | 2026-09-04 | Prices, discount, exchange rate, hours, rates, margins and VAT % are frozen into the costing when used. |
| D-013 | 2026-09-04 | Margin is two percentages, material and labour, with a "same for both" shortcut in the UI. (Arithmetic: see D-036.) |
| D-014 | 2026-09-04 | VAT is a per-company percentage, default 16, applied after margin. |
| D-015 | 2026-09-04 | Money is stored with 2 decimals; rounding happens only at displayed or printed totals. |
| D-016 | 2026-09-04 | Numbering per company per year: CM-YYYY-NNNN costings, QT-YYYY-NNNN quotations, EN-YYYY-NNNN enquiries. (Quotation part superseded by D-035.) |
| D-017 | 2026-09-04 | Lifecycle is draft → submitted → approved. An approver may return a submitted costing to draft; a comment is mandatory and logged. |
| D-018 | 2026-09-04 | After approval any change creates a new revision; older revisions stay approved and read-only. |
| D-019 | 2026-09-04 | Only the approver role can approve a costing and release the PDF quotation. |
| D-020 | 2026-09-04 | Hours on a costing line are editable; the source hours are shown beside the edited value. |
| D-021 | 2026-09-04 | Buyer companies are full tenants in release 1. |
| D-022 | 2026-09-04 | One user belongs to exactly one company and may hold several roles in it. |
| D-023 | 2026-09-04 | Component categories are master-only and seeded: switchgear, busbar, accessories & hardware, fabricated enclosure parts. |
| D-024 | 2026-09-04 | Private components get no discount and no currency conversion; they are priced in the company currency. |
| D-025 | 2026-09-04 | Supabase region is eu-west-2 (London). **Superseded by D-075.** |
| D-026 | 2026-09-04 | All schema changes are numbered SQL migrations under supabase/migrations/; the database must rebuild from scratch. |
| D-027 | 2026-09-04 | Tenant isolation is Postgres row-level security keyed on company_id; shared master rows use company_id IS NULL. |
| D-028 | 2026-09-04 | Frontend is a Vite + React + TypeScript single-page app deployed as static files (Vercel or Netlify). |
| D-029 | 2026-09-04 | Costing totals are calculated in Postgres views and functions; the UI never re-implements the formula. |
| D-030 | 2026-09-04 | PDFs are generated once at release time and stored in a private Supabase Storage bucket. |
| D-031 | 2026-09-04 | Code is organised by concern (auth, admin, library, costing, quotation, crm); no file over ~300 lines. |
| D-032 | 2026-09-04 | Backups: Supabase daily backups and PITR, plus a weekly off-site dump, plus a restore drill before go-live and quarterly. |
| D-033 | 2026-09-04 | First build slice is "cost one panel end to end"; PDF and CRM follow. |
| D-034 | 2026-09-04 | English only in release 1. |
| D-035 | 2026-09-04 | Supersedes the quotation part of D-016: the printed quotation reference is company prefix + sequence + -REVn (e.g. NPP-193-REV1); year optional per company; default prefix QT. Internal costing numbers stay CM-YYYY-NNNN. |
| D-036 | 2026-09-04 | Margins are applied by division, cost ÷ (1 − margin %), as in the existing sheets; the UI shows the equivalent markup. |
| D-037 | 2026-09-04 | A third, optional negotiation margin per costing (default 0) is applied to the whole panel price after material and labour margins. |
| D-038 | 2026-09-04 | The panel unit selling price is rounded up to a company rounding step, default 100 in the company currency; costs underneath stay exact. |
| D-039 | 2026-09-04 | Busbar is a rate-based component: weight per unit × a material rate per kg; material rates follow the master-default-plus-company-override pattern of labour rates. |
| D-040 | 2026-09-04 | Fabricated enclosure and sheet-metal parts are ordinary fixed-price components in the enclosure category; their cost comes from the separate fabrication costing application. |
| D-041 | 2026-09-04 | A costing may offer alternatives: panels carry an optional option label, and the price schedule prints one table with subtotal, VAT and total per option. |
| D-042 | 2026-09-04 | Each panel carries a technical description, enclosure dimensions and unit of measure for Annexure IV; the app drafts the description from the panel's contents. |
| D-043 | 2026-09-04 | Quotation layout follows docs/quotation-template.md, derived from quotation NPP-192-REV1; approver-editable text is frozen with the released quotation. |
| D-044 | 2026-09-04 | The PDF prints amounts in the company currency only, labelled with the company's currency word (e.g. KSH). |
| D-045 | 2026-09-04 | Material rates follow the labour-rate pattern: a master default in KES with a per-company override. Copper busbar is the only rate-based material in release 1. |
| D-046 | 2026-09-04 | Manufacturer data sheets stay outside the app in release 1; the quotation PDF ends with the technical offer. |
| D-047 | 2026-09-04 | Letterhead is one company logo in the header; partner logos (Siemens, C&S) and certification marks are a footer strip any company may fill. |
| D-048 | 2026-09-04 | Follow-up reminders are an in-app due and overdue list in phase 1; no email digest. |
| D-049 | 2026-09-04 | Supabase, the web host and GitHub are owned by the operator's own account; alerts and backup failures go to alp007panchal@gmail.com. |
| D-050 | 2026-09-04 | Data protection is covered by a short plain-language terms page in the app, shown at sign-up, rather than a signed agreement per company. |
| D-051 | 2026-09-04 | The master copper rate is seeded at 3,000 KES per kg and is editable at any time; every change is recorded like a component price change. |
| D-052 | 2026-09-04 | No finished master component list exists, so the library is built and maintained inside the app by Excel upload and download; the round-trip is part of slice 1, not a go-live afterthought. |
| D-053 | 2026-09-04 | An upload matches existing rows by id column, else manufacturer part number within make, else make plus item plus description; it shows a preview of new, changed, unchanged and rejected rows before saving anything. |
| D-054 | 2026-09-04 | Uploads never delete rows; removing a component is a separate deliberate action. Price changes made by upload go to the price history with the user, time and batch. |
| D-055 | 2026-09-04 | Every library list can be downloaded to Excel, and the download doubles as the upload template. |
| D-056 | 2026-09-04 | Vercel hosts the web app. Netlify is equivalent; Vercel is named so the operations guide can give one exact set of steps. |
| D-057 | 2026-09-04 | Supersedes the three-environment plan in D-028's architecture: local and production to start, staging added in slice 6 when there is data worth rehearsing against. |
| D-058 | 2026-09-04 | Supabase free tier during building; move to the paid tier (daily backups and point-in-time recovery) before the first real quotation is released. |
| D-059 | 2026-09-04 | *(key names updated by D-077)* The project URL and browser key are public by design and live in the browser; the service role key and database password live only in GitHub secrets and a password manager, and are never shared. |
| D-060 | 2026-09-04 | Migrations reach production through a GitHub Actions workflow using repository secrets, never from a laptop. |
| D-061 | 2026-09-04 | Planning documents merged to main without a pull request, at the owner's request; work continues on the planning branch. |
| D-062 | 2026-09-04 | Security predicates (`app.current_company_id`, `is_master_admin`, `has_role`) are SECURITY DEFINER and STABLE: definer to stop a policy recursing through the table it protects, stable so they run once per statement. Enforced by a test. |
| D-063 | 2026-09-04 | People are never deleted, only deactivated: their name must stay attached to the costings they built. No delete policy exists on profiles. |
| D-064 | 2026-09-04 | Row-level security says which rows you may touch; a column guard trigger says which columns. Found during slice 0: the self-service clause on profiles let any user set is_master_admin on their own row and read every company. |
| D-065 | 2026-09-04 | user_roles carries a composite foreign key to (profiles.id, profiles.company_id), so a role row cannot name a person from another company. |
| D-066 | 2026-09-04 | company_counters stores last_no, the last number issued, and is written only by app.next_number; nobody holds an update grant on it. |
| D-067 | 2026-09-04 | A quotation number without the year runs its own continuous sequence, matching the existing NPP-192 style where the number counts all quotations ever, not per year. |
| D-068 | 2026-09-04 | The master admin can read every company but cannot edit a company's own quotation wording: support means looking, not editing someone's commercial text. |
| D-069 | 2026-09-04 | Database tests run as ordinary signed-in users against the real policies, not as an administrator, and every "must be refused" assertion states the expected error so a typo cannot masquerade as a working control. |
| D-070 | 2026-09-04 | Inviting a person runs in a Supabase Edge Function, not the browser: creating a login needs the service role key, which bypasses every rule and must never reach a browser. The function checks the caller may invite into that company before using the key. |
| D-071 | 2026-09-04 | supabase/bootstrap.sql creates the first company and master administrator by hand, because only an administrator can add people and at the start there are none. It is idempotent and covered by a test. |
| D-072 | 2026-09-04 | A database connection with no signed-in user (migration, bootstrap, service role) is treated as trusted by the column guard triggers; the API always has a signed-in user. |
| D-073 | 2026-09-04 | Route guards and hidden buttons in the web app are for tidiness only. Row-level security is the security boundary, and every rule the UI applies is enforced again in Postgres. |
| D-074 | 2026-09-04 | The toolchain is Vite, React, TypeScript strict, TanStack Query and plain CSS. No linter and no component library: fewer moving parts for someone learning to operate this, with TypeScript strict catching most of what a linter would. |
| D-075 | 2026-09-04 | Supersedes D-025: the Supabase project lives in eu-west-1 (Ireland), not London. About five milliseconds further from Nairobi, and in the EU rather than outside it. The data-protection note says Ireland. |
| D-076 | 2026-09-04 | The service role key is never collected or stored by us: Supabase provides it to the Edge Function from its own environment. The secrets a person handles are a personal access token, the project ref and the database password. |
| D-077 | 2026-09-04 | Supabase renamed its API keys: `anon` became the **publishable** key (`sb_publishable_…`) and `service_role` became the **secret** key. The app reads `VITE_SUPABASE_PUBLISHABLE_KEY` and falls back to `VITE_SUPABASE_ANON_KEY` for older projects. |
| D-078 | 2026-09-04 | The invite-user function validates the caller's token with the privileged client rather than building a second client from the browser key. One fewer environment variable, and immune to a key rename. |
| D-079 | 2026-09-04 | Two-factor authentication on GitHub, Supabase and Vercel is required before real customer data exists, with recovery codes stored alongside the database password. The app holds other companies' commercial data, so account takeover is the likeliest route to a breach. |
| D-080 | 2026-09-04 | History triggers (component prices, material rates) are SECURITY DEFINER because nobody holds an insert grant on the history tables: history is written by the system as a side effect, so it cannot be forged or tidied away. Found when a price edit failed outright. |
| D-081 | 2026-09-04 | A component is `fixed` or `weight_rate`, enforced by a check constraint so a row can never be half-configured: fixed needs a price and no weight, weight_rate needs a weight and a rate code and no price. |
| D-082 | 2026-09-04 | A master assembly may use only master components, and a private assembly only master or its own; enforced by trigger, so a company can always open its own costings. |
| D-083 | 2026-09-04 | Effective prices and hours live in views (`v_component_prices`, `v_assembly_hours`, `v_material_rates`), so discount, currency conversion and hour overrides are computed in one place rather than in each screen. |
| D-084 | 2026-09-04 | The app is live at cost-matrix-theta.vercel.app, Supabase project `mssqjuzgycfpfmtjukvq` in eu-west-1. Slice 0 verified in production on 2026-09-04. |
| D-085 | 2026-09-04 | Every costing child row carries both its parent and its costing, tied by a composite foreign key, so a row cannot drift into another costing and the policies stay simple and fast. |
| D-086 | 2026-09-04 | Immutability is enforced by the database, not the screens: the write policies require the costing to be a current draft, so a submitted or approved costing cannot be edited even by a direct API call. |
| D-087 | 2026-09-04 | Status changes go through SECURITY DEFINER functions that check the role, check the current status and write the history in the same transaction, so the log can never disagree with the costing. |
| D-088 | 2026-09-04 | `app.create_costing` freezes the company's currency, rate, discount, margins, rounding, VAT and hourly rates at creation. A rate rise next month cannot silently reprice an existing job. |
| D-089 | 2026-09-04 | A revision is a deep copy of the approved costing at the prices it froze, not a re-pricing. Refreshing prices is a separate, deliberate act on individual lines. |
| D-090 | 2026-09-04 | The API client reaches only the public schema, so the six costing functions have thin public wrappers that pass straight through to app. The app schema stays internal; the API surface is exactly what a screen may ask for. |
| D-091 | 2026-09-04 | Every edit in the costing editor reloads the costing from the database afterwards. The screen never computes a total; it shows what the views return. |
| D-092 | 2026-09-04 | Editable fields commit on blur rather than on every keystroke, so a person can type a number without a save firing mid-way, and the database round-trip happens once per change. |
| D-093 | 2026-09-08 | Spreadsheet reading and writing uses exceljs, loaded by dynamic import so its megabyte is fetched only by the person who clicks Download or Upload, never by someone costing a panel. |
| D-094 | 2026-09-08 | Upload matching order: the id column, then part number within the same make, then code, then make plus name. The rules are pure functions with unit tests, because matching is the easiest part of a bulk upload to get wrong. |
| D-095 | 2026-09-08 | The upload reader finds the header row rather than assuming row 1, and skips a sheet's own sub-headings, so the old costing workbooks upload directly without being reshaped first. |
| D-096 | 2026-09-08 | Duplicate codes within one uploaded file are rejected after the first, rather than applied in sequence to the same component, so the outcome does not depend on row order. |
| D-097 | 2026-09-08 | Releasing a quotation is one database function: approver only, approved and current only, issues the family's sequence on first release, freezes wording and letterhead, writes history. There is no insert or update policy on quotations; the functions are the only way in. |
| D-098 | 2026-09-08 | The PDF is rendered in the browser and stored before the release is recorded. If storing fails, nothing is recorded, so a quotation never exists without its document. |
| D-099 | 2026-09-08 | The quotation wording is pre-filled from company_settings and editable by the approver at release, then frozen on the quotation row. Blanks fall back to the company defaults inside the database function, not in the browser. |
| D-100 | 2026-09-08 | Storage buckets `quotations` and `logos` are private; object paths start with the company id and the policies check that first folder. Bucket creation is guarded on the storage schema existing, so the migration also runs on plain PostgreSQL in the test harness. |
| D-101 | 2026-09-08 | Turning a costing into the PDF's data is a pure step (prepare.ts) separate from drawing it (Document.tsx), so option grouping, numbering and wording defaults are unit-tested without rendering. |
| D-102 | 2026-09-08 | Logos are uploaded through the app into the private bucket and embedded in the PDF as data URLs. The reference files in docs/reference/logos are the source copies, not what the app reads. |
| D-103 | 2026-09-08 | @react-pdf/renderer is loaded by dynamic import from a single module (pdf/build.tsx), landing in its own chunk; the main bundle is 266 kB. |
| D-104 | 2026-09-08 | BOM exports read `v_costing_items_by_category`, which already multiplies quantities through assembly and panel quantities; the app groups and formats, never recomputes. Four category exports plus an all-in-one workbook, each as .xlsx or CSV. |
| D-105 | 2026-09-08 | The four category groups are always present in order, even when empty, so the export buttons are stable and an empty category is visibly empty rather than missing. |
| D-106 | 2026-09-08 | CSV is RFC 4180 with CRLF line ends and a UTF-8 byte-order mark, so Excel opens it correctly without an import wizard. |
| D-107 | 2026-09-08 | CRM rows (contacts, projects, enquiries) point at their customer through a composite key (customer id, company id), so a contact can never belong to a customer of another company whatever the row claims. |
| D-108 | 2026-09-08 | Enquiries are inserted only through `create_enquiry`, which issues EN-YYYY-NNNN; there is no insert policy on the table. |
| D-109 | 2026-09-08 | A costing's enquiry is optional in the database but offered first on the new-costing form, and a revision inherits it. Forcing an enquiry on every costing would have blocked a first-time user with an empty CRM. |
| D-110 | 2026-09-08 | The enquiry follows its quotation: marking a quotation sent moves an open enquiry to quoted; won or lost moves it to won or lost. Done inside `set_quotation_status`, so the two cannot disagree. |
| D-111 | 2026-09-08 | The release page picks a customer record, which fills the printed name and address; both stay editable, and the quotation keeps the record ids alongside the printed text. |
| D-112 | 2026-09-08 | Costing engineers may create customers, contacts, projects, enquiries and follow-ups; only enquiry numbering and quotation release are function-only. |
| D-113 | 2026-09-08 | `create_costing` gained an optional enquiry parameter by dropping and recreating the function rather than overloading it, so the API call cannot become ambiguous. |
| D-114 | 2026-09-09 | The owner's zip held raw catalogue CSVs and kit-export workbooks, not the processed files it described. They are committed unedited under `data/raw/` and the seed under `data/seed/` is derived from them by `scripts/build_seed.py` and `scripts/build_kits.py`, so a correction is made once in the raw file or the rule, never by hand in the seed. |
| D-115 | 2026-09-09 | `docs/reference/current-costing-and-quotation-reference.md` §5 (thirteen decisions) is the source of truth over other documents and existing code. Nine decisions are the owner's; 4, 10, 12 and 13 are proposed from the brief and marked so until the owner confirms. |
| D-116 | 2026-09-09 | Adapt, do not rebuild: slices 0–4 stay live; where §5 conflicts with the schema a forward migration (0008 onward) reshapes it. |
| D-117 | 2026-09-09 | One pull request per feature, reviewed and merged by the owner. Supersedes D-078's fast-forward to `main`; migrations reach production only when the owner merges. |
| D-118 | 2026-09-09 | Master EUR conversion is 113 KES per EUR × landed-cost factor 1.7699115 = 200 KES per EUR, back-solved from the NPP-192 Option 1 sheet where every matched catalogue part is priced at exactly EUR × 200. The owner may replace the factor with the real freight-duty-clearing figure. |
| D-119 | 2026-09-09 | Busbar stays priced by kg per metre × copper rate (decision 3) even though the workbook used catalogue metre prices; kg per metre is width × thickness × 8.96 g/cm³. The resulting 18,072 KES difference on Option 1 is an accepted, itemised difference (§6.2). |
| D-120 | 2026-09-09 | Heading rows in the catalogue (no part number and no price, or a part number repeated as description with no category) are skipped; rows kits reference but the catalogue lacks become placeholder parts with no price, flagged, so every kit imports. |
| D-121 | 2026-09-09 | Kit identity ignores case, spaces and commas; the main device is the marked line or else the first line that is not busbar, cable or controls; mislabelled lines are flagged `stray-line`, never moved. The SAMPLE kit workbook is a format sample and is not imported. |
| D-122 | 2026-09-09 | Labour hours per kit group are the owner's to supply through `data/seed/kit-group-labour-template.csv`; the exports carry none. Until filled, the app's selling price is compared with the workbook on material subtotals only. |
| D-123 | 2026-09-09 | Acceptance for the costing engine is the §6.2 table: switchgear 2,386,422.24 and busbar & cable 1,218,288.00 from the seed for NPP-192 Option 1, plus exact margin, rounding and VAT arithmetic on the workbook's material subtotal. |
| D-124 | 2026-09-09 | Session 1 runs on a branch stacked on PR 1 (`…-s1-foundation`, PR 2 targets PR 1's branch) so the owner reviews the seed and the schema separately; GitHub retargets PR 2 to `main` when PR 1 merges. The owner accepted that a §5 correction may mean redoing part of the schema. |
| D-125 | 2026-09-09 | `components.unit_price`/`currency_code` are renamed `purchase_price`/`purchase_currency` rather than kept beside new columns: one price, never two that can disagree. Existing KES rows are purchase KES at factor 1, so nothing reprices; private rows of a non-KES company are marked with that company's currency, which is what they always meant. |
| D-126 | 2026-09-09 | The landed factor is stored with eight decimals (EUR 1.76991150) because 113 × 1.769912 lands at 200.00006 and showed as 2 cents on a 380 EUR cubicle; with eight decimals the error is under a millionth. |
| D-127 | 2026-09-09 | Currency factors follow the material-rate pattern: master rows, optional company override, history by trigger, the company's own row wins in `v_component_prices`. Only currencies with a master row exist; the master admin adds them; a component in a currency without a row is refused at save. |
| D-128 | 2026-09-09 | The enclosure uplift is applied per cubicle line when a kit enters a costing (unit price × (1 + uplift)) and the percentage is frozen on the line and on the costing, so the schedule and the BOM agree and an old costing explains itself. |
| D-129 | 2026-09-09 | Hours precedence is costing line → company override → kit's own row → kit group → zero, resolved in `v_assembly_hours` so `add_assembly_to_costing` needed no change; `source = kit_group` is recorded on the costing labour line. |
| D-130 | 2026-09-09 | The `assemblies` table keeps its name; screens, buttons and messages say "kit". Renaming 200 identifiers would buy nothing and risk the live app. |
| D-131 | 2026-09-09 | The Excel round-trip gained a Currency column (or a currency word in front of the price, KSH read as KES) and an Enclosure cubicle column; a blank currency means KES so old sheets still upload. |
| D-132 | 2026-09-09 | With the eight-decimal factor (D-126) the NPP-192 Option 1 acceptance figures from the seed are switchgear 2,386,421.80 and material 4,012,709.80 (difference −152,288.00 to the shilling); D-123's 2,386,422.24 carried the 0.00006 rounding and is superseded. |
| D-133 | 2026-09-09 | The seed import runs in the database: three functions with an `apply` flag validate, compare and (when true) write in one transaction; the preview is the same function without writing, so the screen and the test suite obey one set of rules. The Excel round-trip stays for ad-hoc edits. |
| D-134 | 2026-09-09 | A part with no purchase price is a placeholder: allowed and flagged so every kit imports, refused by `add_assembly_to_costing` with the part named, and un-flagged the moment a price is entered. |
| D-135 | 2026-09-09 | A kit is rejected whole when it lacks a main device, has two, uses a part the library does not hold, or a non-positive quantity; re-importing a changed kit replaces its lines and keeps its hours and any company overrides. Imports never delete. |
| D-136 | 2026-09-09 | A kit's code is derived from its name (`app.kit_code`: letters and digits, hyphens between) so the seed needs no code column and the same name always maps to the same kit. |
| D-137 | 2026-09-09 | Test 14 loads the real `data/seed` CSVs with `\copy` and leaves them in the throwaway database, so the session-3 acceptance test builds NPP-192 Option 1 on the actual seed. |
| D-138 | 2026-09-09 | A panel has one *components and enclosure* holder (`kind = free`, quantity fixed at 1, no labour) for catalogue components added on their own and for typed lines, so the existing panel → line → item shape carries them and every total, BOM and PDF works unchanged. |
| D-139 | 2026-09-09 | A typed line (`is_manual`) is a name, category, price and quantity that lives in the costing only; it is how the workbook's uncatalogued parts and the enclosure figure are entered until the catalogue has them. |
| D-140 | 2026-09-09 | One function, `app.freeze_component`, prices a catalogue component into a costing; kits and loose components both call it, so the uplift, the frozen workings and the placeholder refusal cannot drift apart. |
| D-141 | 2026-09-09 | `create_costing_revision` had copied costing lines with the 0004 column list and dropped the frozen purchase price, factor and uplift; fixed and covered by a test. |
| D-142 | 2026-09-09 | The NPP-192 arithmetic proof uses one material margin of 28 % (the workbook's ÷0.8 and ÷0.9 combined) because the app has no labour-as-a-percentage; the material figure itself is asserted from the seed to the cent, and the app's category split differs from the workbook's by the 106,800 of cable the workbook filed under its APFC section. |
| D-143 | 2026-09-09 | The technical offer (Annexure IV) is drafted from the panel's own kits and lines by one pure function, on a button while costing and automatically at release when the engineer left it blank; the engineer's text always wins. Grouping is by kit group, then other components, then enclosure. |
| D-144 | 2026-09-09 | The four bills of materials and the price schedule needed no change for sessions 1–3: the BOM view already carries loose and typed lines with their category and the uplifted price, and the schedule reads the panel price view. Checked, not rebuilt. |
| D-145 | 2026-09-09 | Sessions 5 and 6 were a review, not a build: the database already isolated companies, applied the master discount, converted currencies and let the master admin read everything. The gaps were on screen — the master admin's lists mixed every company's rows with nothing to tell them apart — and are closed with a Company column and filter on Costings, Quotations, Customers, Enquiries and People, and a company choice on the invitation. |
| D-146 | 2026-09-09 | The home page shows open follow-ups (overdue first) so the reminder loop starts itself; a customer's details can be corrected in place (released quotations keep what they printed); a company admin edits only kits that belong to their company, on screen as in the database. |
| D-147 | 2026-09-09 | Test 16 asserts what earlier files implied: a private kit built from a master part and a private part, invisible to another company and refused by a master kit; the master admin reads another company's costing, customer and enquiry and cannot change or add to them. |
| D-148 | 2026-09-09 | The owner read §5 and confirmed decisions 4, 10, 12 and 13 as written; all thirteen are now the owner's. Supersedes the "proposed" note in D-115. |
| D-149 | 2026-09-09 | The owner's own reference document and cleaned seed (`costmatrixdocsandseed.zip`) replace the draft document and the derived seed. §5 is the owner's thirteen decisions verbatim; my proposed 4, 10, 12 and 13 (labour by hours, lifecycle, four BOMs, external companies) remain true and stay recorded here (D-001, D-016–D-018, D-003/D-004/D-008), not in §5. Supersedes D-115's reading of §5 and D-148. |
| D-150 | 2026-09-09 | Decision 2 as written: the landed-cost factor is **one number per purchase currency** (EUR 200 = KES per 1 EUR landed). `currency_factors` no longer splits it into an exchange rate and a multiplier; D-118 and D-126 are superseded. The company's own exchange rate (KES per company currency, decision 13) is a separate, unchanged setting. |
| D-151 | 2026-09-09 | Decision 3 as written: the copper rate is 15 EUR per kg and goes through the EUR landed factor like every other price (15 × 200 = 3,000 KES/kg, so nothing reprices). `material_rates` carries a currency; the kg-per-metre table is the busbar components' kg per metre, seeded as catalogue EUR ÷ 15 (100×10 = 9 kg/m, 30×10 = 2.8), which is what the workbook used. D-119 superseded. |
| D-152 | 2026-09-09 | Decision 1 as written: the workbook's enclosure line (102 × 4,000) is disregarded; the acceptance test no longer types it in and asserts switchgear, busbar and accessories from the seed. |
| D-153 | 2026-09-09 | Kit groups are the owner's 17 **labour groups** (ACB split into frame 1 and frame 2), taken from `kit-labour-template.csv`, which also names each kit's main device; the export's section header is kept only as a label. Per-kit hours in that template import as overrides. Decision 11 as written. |
| D-154 | 2026-09-09 | The importer reads the owner's files as they are: `components.csv` in the supplier template columns with `category-map.csv` for the BOM category (a `BOM category:` note on a row overrides it), `kits.csv` with `kit-labour-template.csv`, and the wide `kit-group-labour-template.csv`. My derivation scripts are retired; the owner's pandas scripts reproduce the seed from `data/raw`. |
| D-155 | 2026-09-09 | Importer details for the owner's files: a kit line repeated within one kit is merged (quantities added) with a warning; when the kit template names no main device the first line that is not busbar, cable or controls is taken; rating and poles come from the kit name (`630A`, `50KVAR`, `TP`/`FP`). |
| D-156 | 2026-09-09 | Decision 4 in the app: a panel's card shows the APFC bank's total kVAr as rating × quantity of its kVAr-rated kits; no configurator in phase 1. Test 15 asserts the owner's seed figures (3,622,781.80) with no enclosure line. |
| D-157 | 2026-09-09 | A stacked PR merges into the branch it targets, not into `main`, unless that branch is deleted first; PRs 2 and 3 merged that way, so PR 7 carries sessions 1 and 2 to `main`. From now on: retarget the next PR to `main` right after each merge (or delete the merged branch), and check the "Deploy database" run on `main` before telling the owner a migration is live. |
| D-158 | 2026-09-09 | Blank Components page after the first production import (PR 8, straight to `main`): `money()` and `percent()` accept null and show a dash; `ComponentPrice.unit_price` is typed nullable; an error boundary inside the layout turns any future render crash into a message with the navigation still usable; render tests load the owner's seed from `data/seed` and draw the Components list, the Kits list and a kit built on an unpriced part. A hotfix for something live goes as its own PR to `main`, not onto the stacked branches. |
| D-159 | 2026-09-10 | A person may be moved between companies or removed altogether only while nothing anywhere names them; the count is generic over every `created_by`, `user_id` and similar column, so a table added later is included without another migration. From the first record onwards, deactivate only: a costing must never name nobody. |
| D-160 | 2026-09-10 | Edge Function failures show the sentence the function wrote, not "non-2xx status code"; and creating a company refreshes the session's company list, so the People screen sees it without a reload. |
| D-161 | 2026-09-10 | Invitations go through the owner's own SMTP sender (operations B9c), not Supabase's built-in one, which allows only a few messages an hour and often lands in spam. A **free Gmail account with an app password** is the sender for now: no paid mail product is owned, and the app sends only login and password-reset emails, never quotations. A `neiltd.com` sender before external companies are invited. The credentials live in Supabase alone; they never reach the repository or a chat. |
| D-162 | 2026-09-10 | The costing screen reads in one column, top to bottom — totals, panels, bills of materials, history — instead of a two-column grid that hid the totals beside the panels on a narrow screen. The history names the operator through a view (`v_costing_history`) that left-joins `profiles`, not a foreign key: a foreign key with `on delete set null` would erase the very fact an audit log exists to keep, so a row whose person has gone reads *Someone no longer here*. |
| D-163 | 2026-09-10 | A panel is built in sections (Incomer, AVR bypass, ATS, 2nd incomer, Outgoers, Accessories, APFC bank, or anything typed): free text on the line with a seeded list offered, never a foreign key, so a panel is never blocked by a name nobody thought of. The one loose-parts holder per panel becomes one per section. Sections change no price; they group the screen, carry a subtotal each and give Annexure IV its headings (falling back to the kit group). The bills of materials stay grouped by category, which is what parts are bought by. |
| D-164 | 2026-09-10 | A costing or a single panel can be copied as a **new job** (own number, own family, revision 0, source untouched), which is not a revision. A copy **re-prices at today's rates** through the same `freeze_component` every other line goes through: quoting a new customer at an old copper price is the failure the app exists to prevent. Typed lines keep their typed price; a part that has left the catalogue or lost its price is copied at the old price and named in the returned report. Hours are the engineer's design and are copied as they were, at the new costing's rates. `app.write_history` is the one way an ordinary function writes to the log, still checking the caller's company. |
| D-165 | 2026-09-10 | Won and lost are decided once, on the **enquiry**, naming the quotation that won; the other offers of that enquiry become `superseded`, a new quotation status, because they were never turned down. The reason for a loss is kept on the enquiry as well as on every offer. `set_quotation_status` still decides a quotation whose costing has no enquiry. The quotations screen groups enquiry → offer → revisions, newest first. |
| D-166 | 2026-09-10 | Busbar is left out of Annexure IV: it is how the board is built rather than what the customer is choosing between, and printing its sizes and metres tells a competitor how the board is made. Cable is filed under accessories and still prints. Panels whose description was drafted before this change keep their text — press *Draft from the kits* again to redraw them. |
| D-167 | 2026-09-10 | An enquiry keeps its files (drawings, specifications, emails) in a third private bucket, `attachments`, with the same company-folder rule as quotations and logos, and a row per file whose composite foreign key `(enquiry_id, company_id)` makes filing against another company's enquiry impossible. The upload happens before the row and is undone if the row is refused. An enquiry now has a page of its own at `crm/enquiries/:id` showing what was asked for, its costings, its quotations and its files — the gap left open in the session 6 review. |
| D-168 | 2026-09-10 | The nine notebook changes are merged and live (migrations 0013–0017). The documents are brought back to what is true: `open-questions.md` no longer asks for pull requests to be reviewed, `build-plan.md` marks every session merged, and `acceptance-test.md` — the click-through the owner runs — covers sections, copying, the named history, the enquiry decision, the files on an enquiry, and Annexure IV without busbar. A script that describes a version nobody is running is worse than none. |
| D-169 | 2026-09-10 | Two tracks from now on. `main` is the **basic app**: production, always releasable, the back-up — bug fixes, trial findings, labour hours, prices, the enclosure uplift rule. A long-lived `advanced` branch is the **advanced app**: all foundations (F1–F11) and AI-assistant work, against a separate **CostMatrix Staging** Supabase project, so production data is never touched. An advanced feature reaches `main` only when tests and the NPP-192 acceptance test pass on staging, it sits behind a per-company switch that is off by default, and the owner has tried it on the preview URL; `main` merges into `advanced` after every change to `main`. Recorded in `docs/reference/two-track-setup.md`. |
| D-170 | 2026-09-10 | Advanced-track migrations are numbered from **0100**; the basic track carries on at 0018. Two branches that each add a `0018` would, on meeting, give Supabase two migrations of the same recorded number and the second would be **silently skipped** — its tables would simply never exist. Reserving a range makes the collision impossible rather than merely unlikely. |
| D-171 | 2026-09-10 | "Deploy database" chooses its target by branch as **two separate jobs** (`main` → production, `advanced` → staging), not one job that picks its secrets into an environment: separate jobs mean a run on one branch cannot reach the other's credentials whatever anyone later adds to the file. The staging job refuses to run if its project ref is unset or equals the production ref. |
| D-172 | 2026-09-10 | The staging project is created **by code** (`create-staging.yml`, run by hand), not by hand in the dashboard: it finds the production project's organisation and region, reuses a project already named `CostMatrix Staging`, **wakes it when Supabase has paused it** (free projects pause after about a week), applies migrations, creates the first login with a random password it masks and discards, runs `bootstrap.sql`, and loads `data/seed`. It asserts the resolved ref is not production *before the first write*, and prints the ref, URL and publishable key but never the secret key or a password. A failed restore call stops the run with the dashboard link to press, rather than guessing. |
| D-173 | 2026-09-10 | The part of `14_seed_import.sql` that reads the five seed CSVs into jsonb is extracted to `supabase/seed-csv-load.sql` and shared with `supabase/seed-from-csv.sql`, which is how staging is seeded: one copy rather than two that drift. The staging seed acts as the master administrator by setting `request.jwt.claims`, the same impersonation the test fixture uses, because the importers check `auth.uid()` and an automated connection has no signed-in user. |
| D-174 | 2026-09-10 | F1 adds **no `manufacturer_part_number`**: `components.code` is already the app's key (`100X10MM`, descriptive) and `components.part_number` is already the manufacturer's reference. A third column would be empty on all 735 rows and would split BOM-import matching across two fields. `supplier` is added; `manufacturer` stays as the "Make" the quotation prints, i.e. the roadmap's *brand*. |
| D-175 | 2026-09-10 | F1/F2 add `status` to components and kits, back-filled, but the existing booleans stay **authoritative** in phase 1, held in step by a trigger. `components.is_placeholder` is load-bearing — the `components_pricing_fields` constraint allows a null price only for a placeholder and `app.freeze_component` refuses an unpriced non-placeholder — so flipping authority now would change behaviour for no gain. A later migration flips it once the screens use `status`. |
| D-176 | 2026-09-10 | F3's rate history is `labour_rate_history`, a trigger-filled table, **not** `labour_rates.valid_from`. `components`, `material_rates` and `currency_factors` all keep a current value plus a history table; effective dating exists nowhere in this schema, and `labour_rates` is the only one of the four with no history at all. Consistency beats the roadmap's wording. |
| D-177 | 2026-09-10 | F2's kit-composition freeze — "the single most important foundation" — is **already built**: `add_assembly_to_costing` snapshots the kit's code and name and copies every line into `costing_items` with its price frozen, and `source_assembly_id` is `on delete set null` provenance only, so editing or deleting a master kit cannot reach an existing costing. F2 therefore reduces to recording which version was copied (`costing_assemblies.source_version`) and **needs no back-fill**. A test asserting a master-kit edit leaves an existing costing byte-identical is added anyway. |
| D-178 | 2026-09-10 | F6's `activity_log` is added **beside** `costing_history`, not as an extension of it, against the roadmap's "extend rather than add a second": `costing_history.costing_id` is `not null`, so it cannot carry an enquiry, a component or an AI proposal, and it is read by `v_costing_history`, which the costing screen depends on. `costing_history` stays exactly as it is. |
| D-179 | 2026-09-10 | F10's typed key–value table is **`company_options`**, not `company_settings`: that name is already a wide table of letterhead, wording and commercial defaults. F4's option grouping adds `is_option` only and reuses the existing `costing_panels.option_label` as the group — a second grouping column beside one that already drives `v_costing_option_totals` would be a trap. |
| D-180 | 2026-09-10 | The AI assistant's server side is to live in **Supabase (an Edge Function)** rather than a Vercel function, so `ANTHROPIC_API_KEY` stays with the other server secrets and `web/vercel.json`'s catch-all rewrite needs no surgery; the owner's preference, confirmed unless a blocker appears. Settled at Step 3. |
| D-181 | 2026-09-10 | The first "Create staging project" run failed at `POST /v1/projects` with HTTP 400, and the body said why: Supabase allows **two active free projects per person** and there were already two. Not the organisation reference, the region, the `plan` field or the password rules — all four were accepted. The remedy is the owner's (pause, delete or upgrade), so the script now explains the limit in plain words, lists the projects holding the slots, and takes an optional **existing project ref** to adopt instead of creating one — the same migrations, login and seed applied to a project that already exists, still refused if that ref is production. Every Management API call now prints Supabase's own `message` and the whole body on any non-2xx, and the api-keys call checks its status at all, which it did not before. |
| D-182 | 2026-09-10 | Staging moves to a **second Supabase account**, the first having reached its two-free-project limit (D-181), and that turns out to be the stronger arrangement: the new secret `SUPABASE_STAGING_ACCESS_TOKEN` is the only token the advanced track uses — for every Management API call and for `supabase link`, `db push` and `functions deploy` — and it **cannot see the production project at all**, so the separation is out of reach rather than merely enforced. The production token is never placed in a staging job, nor the staging token in a production one. Two things production used to supply are now supplied otherwise: the organisation comes from that token's own (`GET /v1/organizations`; one, `CostMatrix Staging Org`, and if ever several the one of that name wins or the run stops and lists them), and the region is a workflow input defaulting to `eu-west-1`, ignored when an existing project is reused or adopted. The production guard is now purely the ref comparison against `SUPABASE_PROJECT_REF`, checked *before* the adopt lookup so the answer is "that is production" rather than this account's more confusing "no such project"; that secret is required, because an unset one would make the guard pass vacuously. |
| D-183 | 2026-09-11 | The app had **no page for setting a password**: `SignInPage` has always sent `resetPasswordForEmail` to `/reset-password`, and no such route existed, so every recovery link landed on "No such page" — found on staging, but true of production since slice 0. The page is added at that same address, deliberately **outside `RequireAuth`**: somebody following an invitation has a session but may not belong to a company yet, and the guard would stop them with "your account has not been added to a company yet" — true, and useless when all they need is to choose a password. It names a used or expired link with Supabase's own reason, refuses a mismatch or fewer than eight characters before calling Supabase, and repeats whatever Supabase refuses. The `invite-user` function's `redirectTo` moves from `{SITE_URL}/` to `{SITE_URL}/reset-password`: sending an invited person to the app root signed them in with no password of their own and no way to choose one. |
| D-184 | 2026-09-11 | A **Check staging** workflow counts the staging library (components, kits, lines, groups, unpriced parts, main devices, busbar weight rows) and prints it as a job summary. Read-only — SELECTs and nothing else — so it is safe at any time, and it exists because the staging access token is a write-only secret: nobody outside a workflow can query that database, by design. `supabase/check-staging.sql` is plain SQL that runs on any migrated database, so it is verified locally against the real `data/seed` rather than only in production; the connection-string resolution moves to `scripts/staging/db-url.sh`, shared with the staging seed, so one place knows how to reach that database. |
| D-185 | 2026-09-11 | `status` on components and kits is a **generated** column, not a real one kept in step by a trigger as D-175 proposed: derived from `is_active` and `is_placeholder`, it cannot drift, needs no trigger, and an attempt to write it raises *"column status can only be updated to DEFAULT"* rather than silently reverting. The cost is that the roadmap's third kit state, `draft`, is unreachable until a later migration makes the column authoritative, which is the right trade while the booleans are what the constraint and `app.freeze_component` depend on. Refines D-175. |
| D-186 | 2026-09-11 | There is **no costing-wide productivity factor**, only `costing_panels.productivity_factor`. The roadmap called the costing-level one optional; two factors that multiply together are two places to look when a labour figure surprises somebody, and a job-wide adjustment is the same number typed on each panel. A default for it can live in `company_options` (F10) later. |
| D-187 | 2026-09-11 | The productivity factor is applied in `v_costing_panel_costs`, and the unadjusted figures are kept beside it as `labour_cost_standard` and `hours_standard` — the same rule the frozen price columns follow: a number that has been adjusted must always be explainable. It multiplies hours as well as cost, because the factor is about how long the work actually takes. Material is untouched. |
| D-188 | 2026-09-11 | `v_component_prices` gains the new F1 columns rather than the component form running a second query for them, and both it and `app.copy_panel` were **derived from the existing text by insertion rather than retyped**, so the pricing arithmetic and the re-pricing behaviour they are trusted for are provably unchanged. `create or replace view` can only append columns, which is why the three existing cost columns keep their names and positions. |
| D-189 | 2026-09-11 | Every costing line carries `origin` (manual / kit / configurator / import / ai_proposal) and `origin_ref`, which is a **bare uuid**, not a foreign key: it names an assistant proposal, an import job, or nothing, and a foreign key could only ever point at one table. `app.freeze_component` gains two trailing parameters with defaults, so the callers that do not care need no change; its old signature is **dropped first**, because two functions of one name whose extra arguments have defaults cannot be told apart (the lesson of 0014). Existing rows back-fill to `kit` where the holder is a kit and `manual` otherwise. |
| D-190 | 2026-09-11 | `documents` replaces `enquiry_attachments`: one table for every file the app keeps, keyed by `entity_type` + `entity_id`. The composite foreign key that stopped a file hanging off another company's enquiry cannot exist on a generic table, so the same two guarantees are triggers: `documents_check_entity` refuses a record that is not the company's, and `documents_follow_entity` deletes the rows when their enquiry, costing or quotation goes. Existing rows are copied with their **paths unchanged**, so nothing in storage moves; new uploads go under `{company}/{entity_type}/{entity_id}/…`, and both keep the company first, which is all the storage policy reads. |
| D-191 | 2026-09-11 | `import_jobs` and `import_rows` are filled **by trigger from `import_batches`**, so the three validated importers of 0010 are not touched and their reports are byte-identical; every batch that ever ran gets a job row too. For those three, `import_rows` holds only the rows the report names — rejected and warned — while every future importer (price lists, BOMs, the assistant's mapping) writes every row directly. `import_batches` stays because two columns point at it. |
| D-192 | 2026-09-11 | The approval engine exists and is **not called**: `app.evaluate_approval_rules` returns the first active rule whose conditions all hold, with the facts it looked at, and `submit_costing` / `approve_costing` are unchanged. Every company is seeded one rule, "Always require an approver", and a new company gets it at birth. Two safety choices: no rule holding means an approver is required (a company that switches every rule off does not start self-approving), and a condition naming a fact the engine does not know **never holds** (a typo cannot auto-approve). |
| D-193 | 2026-09-11 | `company_options` holds the assistant's four settings per company — `ai_enabled` **false**, `ai_monthly_token_budget`, `ai_price_age_warning_days` 90, `ai_min_margin_pct` from the company's margins — seeded for every company now and at birth. A company admin changes the thresholds; only the master administrator flips `ai_enabled` (spec A2), enforced by a trigger rather than a policy so the one row is not split in two. The trigger stands aside when no user is signed in, which is a migration or the fixture seeding defaults, not a person. |
| D-194 | 2026-09-11 | `costings.price_snapshot_at` is set to now by `create_costing`, **carried unchanged** by a revision (its prices are the same ones) and therefore fresh on a copy (which re-prices through `create_costing`); existing costings are back-filled to their creation time. `quotations.valid_until` is set at release from `validity_days`; the printed wording in `terms.validity` is untouched. The nightly expiry job and an `expired` status are roadmap 2.6, where the roadmap puts them. |
| D-195 | 2026-09-11 | Text extraction is a third Edge Function, `extract-document`, following `invite-user`: the document is looked up with the **caller's own token** so row-level security decides what they may read, then the file is downloaded with the service role and the text written back. PDF by `unpdf` (first 40 pages, AI spec §4), Word and Excel by unzipping their XML, plain text as is; anything else is `unsupported`, and any error is `failed` with its reason — a status on the row, never a crash. The browser asks for it after every upload, fire and forget, and the screen offers **retry**. **Not run against real files from this environment**, which has no Deno and no Supabase project; the first real run will say what happened rather than hide it. |
| D-196 | 2026-09-11 | The assistant may read **all** attachments on an enquiry or a costing (AI spec §11, point 4, default kept). A per-document confidential flag that would keep a file away from the provider is a later phase; when it comes it is one column on `documents` and one condition in `get_document_text`, nothing structural. |
| D-197 | 2026-09-11 | "Deploy functions" becomes branch-aware **exactly as "Deploy database" is** (D-171): two separate jobs, `main` → production with the existing secrets untouched, `advanced` → staging with `SUPABASE_STAGING_ACCESS_TOKEN` mapped onto the name the CLI reads and the project ref from the `SUPABASE_STAGING_PROJECT_REF` variable, refusing to run if that ref is production's. Until now the functions only reached staging through a re-run of "Create staging project", so `extract-document` — merged in PR 26 — did not exist there; the gap was flagged twice before this closed it. Numbered from D-196 because `advanced` already holds D-185 to D-195 and the fold-in must not collide. |
| D-198 | 2026-09-11 | The assistant's heavy lifting is **SQL, not TypeScript**: migration 0103 adds read-only `security invoker` functions (`costing_snapshot`, `enquiry_snapshot`, `document_text`, `search_kits`, `search_components`, `kit_detail`, `company_policy`, `price_preview`, `assistant_allowance`) and the nine tools of AI spec §5 are thin calls to them with the signed-in user's own client. Row-level security therefore answers every "may the assistant see this?" exactly as it does for the screen — a costing of another company comes back null and the tool says "not found" (acceptance test 3, proved in `25_assistant_queries.sql`) — and `price_preview` is proved to the cent against `v_costing_panel_prices` rather than re-implementing the arithmetic in a second language. The function **never uses the service role**, unlike `extract-document`, because nothing it does needs more than the caller has. |
| D-199 | 2026-09-11 | One seam, `_shared/ai/provider.ts`, with deliberately vendor-free shapes; `anthropic.ts` is the only file that knows the SDK exists, and `fake.ts` is a scripted provider that passes the same loop and tool tests (acceptance test 5) and doubles as `AI_PROVIDER=fake`, a dry run on staging that calls nobody. The core has **no Deno in it** — the Edge Function hands in the key and the client — so `web/`'s vitest runs all of it (42 tests) and CI needs no API key. Model names are settings (`AI_MODEL`, default `claude-opus-5`; `AI_MODEL_FAST`, default `claude-haiku-4-5`, used for plain questions), never code. |
| D-200 | 2026-09-11 | The budget decision is taken **before any provider is constructed**, from `assistant_allowance()`: switched off → 403; no budget set → 403 (nobody spends money by omission); twenty requests a minute per person → 429; the month's tokens past the budget → 429, with a warning from 80 % (spec §4, §11 point 3). Usage is summed from `assistant_messages` itself, so the figure the refusal quotes cannot drift from the log. Acceptance test 6 is therefore two halves: the SQL test shows a 1,000-token budget read as exhausted after one 1,100-token turn, and the vitest shows the refused path sends the provider nothing. |
| D-201 | 2026-09-11 | The loop is a hand-written one over the seam rather than a vendor runner, capped at **12 model ⇄ tool rounds and about 170 seconds**, every tool call of one turn run and every result handed back together with `is_error` where it failed, because dropping one silently teaches the model to stop calling tools in parallel. `create_proposal` is the only write, validated against the §6.3 shapes by hand (no schema library in the function) so a malformed proposal is a tool error in words, never a half-right row. Earlier turns are replayed as text only; tool traffic is logged, trimmed, not replayed. A model refusal is a `refused` event and a stop, not an error, and nothing is applied either way. |
| D-202 | 2026-09-11 | Server-side refusal fallbacks are **on by default** (`fallbacks: "default"`, the scalar form, so there is no model list to keep current) with `AI_FALLBACKS=off` as the one-setting escape hatch; prompt caching marks the system prompt, which is built static-first and with sorted keys so the prefix is byte-stable. **Not run against Anthropic from this environment**, which has no Deno, no project and no key: the request shape in `anthropic.ts` is the one part verified only by the first real turn on staging; everything else is covered by 60 new database assertions and 42 vitest tests. |
| D-203 | 2026-09-11 | The two default model names were **checked against Anthropic's current list before the first live call** and both stand: `claude-opus-5` for drafts and reviews, `claude-haiku-4-5` for plain questions. No default changed. The price table in the adapter was checked with them (Opus 5 at 5 / 25 US$ per million tokens in and out, Haiku 4.5 at 1 / 5) and gained a row for `claude-fable-5-1` at 10 / 50, so a company that sets `AI_MODEL` to it still gets a true cost column rather than Opus's. The owner asked for this because a wrong default would look exactly like a broken key on the first run. |
| D-204 | 2026-09-11 | A failed live call shows **the reason and what to do about it**, never a generic error: the adapter maps Anthropic's typed errors (and a 402, which has no class of its own) onto a code — `no_credit`, `bad_key`, `unknown_model`, `not_allowed`, `rate_limit`, `unreachable`, `bad_request` — the code travels through the stream to the panel, and `events.ts` pairs it with a remedy in the team's words. The commonest first-run failure, no credit, reads "the Anthropic account has no credit left" with "an administrator tops it up at console.anthropic.com → Billing. Nothing in CostMatrix needs changing, and nothing has been lost", because that is true and because the opposite — a red box saying "error" — is what the owner specifically objected to. The four refusals decided before any provider exists (off, no budget, budget spent, rate) carry codes the same way. |
| D-205 | 2026-09-11 | Applying a proposal calls **the ordinary engine functions** — `add_assembly_to_costing`, `add_component_to_costing` — from one `security invoker` function, `app.apply_proposal` (migration 0104), and then stamps `origin = ai_proposal` and `origin_ref` on what they created. So an assistant line is priced and frozen by exactly the code a hand-picked line is, and `26_assistant_apply.sql` proves it to the cent; a second pricing path in TypeScript would have been a second place for the arithmetic to drift. The apply step also never loosens a rule: an approved costing refuses it, another company cannot see the proposal, and a person without `can_edit_costings` is refused. Where the engine merges a component into a line a person already put there, the proposal records `merged` rather than claiming that line's provenance. |
| D-206 | 2026-09-11 | **Low-confidence lines start rejected** in the proposal card and High and Medium start accepted (AI spec §3.2). Apply sends only the accepted ones, so the path of least effort applies what the model was sure about and leaves its guesses for a person to look at; accepting a Low line is one click, and the card's button always names how many lines it is about to apply. A partly applied proposal is `partially_applied`, not `applied`, and can still be dismissed — what was applied stays applied. |
| D-207 | 2026-09-11 | The **Assistant screen** (company admin) holds the switch, the monthly token budget, the two review thresholds and the usage: six months of tokens, this month by person, the proposal counts, and the month's cost in dollars. The switch is shown to everyone but only the master administrator gets the button, because the trigger of D-193 would refuse anyone else and a button that always fails is worse than none. Usage comes from one read-only function, `app.assistant_usage`, so the figures on screen are the same ones the budget stop uses. |
| D-208 | 2026-09-11 | Acceptance test 1's "within 5 %" is measured against **3,519,031.80 — the seed acceptance figure less the 103,750 of items the draft cannot match** (two synchro-check relays, timers and relays, two fan-and-filter units), because a draft lists those as unresolved for a person to create rather than pricing them, which is the behaviour the same test asks for two lines earlier. The kit-based draft of the trial build sheet gives **3,397,266.80**, 3.5 % under that; the remaining gap is kit composition against the workbook's line-by-line list, the same kind of difference `npp192-acceptance-from-seed.md` already itemises. Both figures are asserted, so neither can drift unnoticed. |
| D-209 | 2026-09-11 | A supplier price list is an **import job in the F9 framework** (0102), not a new mechanism: one `import_jobs` row of type `price_list`, one `import_rows` row per line of the file with what it matched and what would happen, and a review screen where a person accepts rows. The preview writes nothing but those two tables — the assertion the whole feature exists for, and the one test 27 makes first — so an upload can be read, argued about and discarded without touching a price. Accepting is per row or all at once; a job stays `preview` until no change is left, then becomes `applied`. |
| D-210 | 2026-09-11 | A price-list row is matched in **four attempts, most trustworthy first**: our own code, the manufacturer's reference exactly, the make plus the reference with punctuation ignored, then the reference with punctuation ignored against either field. `match_method` is kept on the row so the review can say in words how it was found. Two parts answering to one reference is a **warning naming the count**, never a guess — the seed does hold such pairs once punctuation is stripped. A reference nobody has is `new` with "add the part first": an upload may re-price the catalogue, never extend it. |
| D-211 | 2026-09-11 | Four things are refused at the **preview**, where they cost nothing, rather than at the save: a blank reference, a cell that is not a number (quoted back as the supplier wrote it, so "on request" is visible rather than read as zero), a negative price, and a currency with no master landed factor — that last with the same sentence the database trigger would have raised, naming the currency and who can add it. A busbar size, priced by weight, is a warning pointing at the copper rate, because a list price cannot apply to it. |
| D-212 | 2026-09-11 | An accepted row sets `price_valid_from` from the list's own date and `price_source` to "supplier · file name", and `app.record_component_price_change` — derived from the 0008 text by insertion — now writes that source into `component_price_history.source`, the column 0100 added and nothing filled. So a price in the history says where it came from whatever route it took. A placeholder that gets a real price stops being a placeholder in the same statement, which is what lets a costing use it. |
| D-213 | 2026-09-11 | The file is read in the **browser** and matched in the **database**: CSV by the existing reader, Excel through a generic sheet reader that finds the header row, and PDF by uploading it as a `documents` row of type `supplier_price_list` (a type 0101 already allowed, with no entity) and reading the text `extract-document` extracted. The PDF parser is deliberately crude — a line with a reference-looking token and a number — because every row is shown before anything is saved, and a PDF nobody can parse is better said out loud ("ask for a spreadsheet") than guessed at. The column mapping is guessed from the headers and corrected by the person, and kept on the job in `column_mapping`. |
| D-214 | 2026-09-11 | **F12, physical dimensions**, is added now as groundwork for the panel layout canvas (phase 3.8, recorded in the roadmap in the same PR): the canvas cannot be built until the library knows how big a device is and how much room it needs around it, and measuring 735 parts is the owner's work, which can start as soon as the columns exist. Every column is nullable, nothing in the engine reads them, and the one thing that does — the space check — says "not known yet" rather than guessing. Phase 4.2 ("enclosure fill check from dimensions in attributes") is struck from the roadmap as brought forward: structured columns beat a free-form `attributes` key for something the canvas will read on every drag. |
| D-215 | 2026-09-11 | Dimensions are **columns, not `attributes` keys**: `width_mm`, `height_mm`, `depth_mm`, `mounting_type`, `weight_kg`, plus `clearances` as one jsonb object of the four sides. A clearance is part of the space a part really takes, so the footprint functions add it rather than leaving each screen to remember; `mounting_type` is constrained to the six zones a cubicle actually has, so the canvas can sort a part into one without a lookup table. An `enclosure_layout` may only be set on a part marked an enclosure cubicle, enforced by a check constraint — a usable internal area recorded against an ACB would be nonsense that the canvas would later have to defend against. |
| D-216 | 2026-09-11 | A kit's footprint is **derived, with an override**: `app.kit_footprint` returns the kit's own `footprint_*` when set, else the main device's size plus its clearances, else "not known, because that device has no size yet" — naming the device, so the owner knows what to measure next. Most kits are one device on a plate, so deriving is right for most of the 296; the override exists for the kits whose accessories sit beside the device. |
| D-217 | 2026-09-11 | The fit check is **area against area**, not geometry: `app.panel_fit` compares the footprints on a panel (× quantity) with the usable area of the cubicles costed on it, times a per-company `layout_safety_factor` seeded at **1.3** for wiring, clearances and getting a spanner in. Four verdicts — fits, tight (over 90 %), no_fit, unknown — and unknown whenever either side is unmeasured. It is advisory: read-only, shown as one line on the panel, silent while nothing is measured, and it changes no price, hour or total. Real placement is the canvas's job in 3.8; a quoting engineer only needs to know whether enough cubicle has been bought. |
| D-218 | 2026-09-11 | The dimensions arrive through a **separate importer**, `app.import_dimensions`, reading `data/seed/dimensions-template.csv` — part numbers and descriptions pre-filled by `scripts/build_dimensions_template.py`, the measurements blank. It touches only the F12 columns, never a price or a category, because a template with no price column run through `import_components` would turn every priced part into a placeholder. A blank row counts as "not filled in yet" rather than a rejection, so the owner can fill the file a few parts at a time; re-running the generator keeps what has already been typed. `import_components` also accepts the new columns when a file carries them, and ignores them when it does not — which is why test 14 passes unmodified. |
| D-219 | 2026-09-11 | A parts list somebody else wrote is the **same import job** as a price list (F9), with a different question asked of each row: not "what does this cost now?" but "what would this be on a costing?". `app.match_catalogue_row` runs the four attempts of 0105 over everything the caller can see — the master catalogue *and* their own parts — because a BOM names devices rather than one supplier's library. Preview writes only `import_jobs` and `import_rows`; the costing is untouched until a person chooses. |
| D-220 | 2026-09-11 | Where a matched device is the **main device of exactly one kit**, that kit is what the row proposes, because a kit is the unit this company costs with (reference §5, decision 11): the bare MCCB has no busbar, no cable, no accessories and no labour. Where several kits share the device the row proposes the part and lists the kits for the person to choose; where none does, the part itself. The choice is always a person's — the screen shows the kit first, with any unpriced part in it named. |
| D-221 | 2026-09-11 | A row that matched nothing offers a **placeholder**, and creating one stops there: the part is added to the company's library unpriced, and **no costing line is made**, because `app.freeze_component` refuses an unpriced part and is right to. The row says "price it, then add it to the costing". A panel is created only when a line actually needs one, so a run that only fills the library leaves no empty panel behind. |
| D-222 | 2026-09-11 | Adding to the library stays a **company administrator's** job, refused in `apply_bom_import` with a sentence naming who can do it rather than letting the row-level policy answer in its own words. But **running a parts list is not**: the two import policies of 0102 gain one clause each — a `bom` job of this company for somebody who may edit costings — because the lines it creates are ordinary costing lines the same engineer could have added by hand. Nothing else about those policies changes. |
| D-223 | 2026-09-11 | Imported lines land on a **panel of their own** with `origin = import` and `origin_ref` = the job, through the ordinary engine functions, so they are priced and frozen exactly as hand-added ones are. `app.add_line_with_origin` is 0104's `apply_proposal_line` with the origin as an argument, including its merge rule: a component already in that section gains quantity instead of becoming a second line, and a line somebody put there by hand keeps its own provenance. |
| D-224 | 2026-09-11 | The rules engine of 0102, built and deliberately called by nobody, is now called by `submit_costing` and `approve_costing` — and **the default rule keeps today's behaviour exactly**. Every company has one rule, "Always require an approver", so until somebody writes another one nothing about submitting or approving changes; the whole existing suite passing unaltered is the proof. What is added: `block` refuses the submission naming the rule, `auto_approve` approves on the spot, `require_master_admin` refuses an ordinary approver, and the rule that decided is written into the costing history so the record says why. |
| D-225 | 2026-09-11 | A costing that a rule approved records `approved_by = null` and a history line of its own, "approved by rule", naming the rule. Putting the submitter's name in `approved_by` would say a person approved it when no person did; the history and the null together are the truthful record, and the screen can say "approved automatically" rather than inventing an approver. |
| D-226 | 2026-09-11 | `require_master_admin` means **a master administrator who belongs to that company**. `approve_costing` finds the costing by `company_id = app.current_company_id()`, and the master administrator's access to another company's data is read-only by design (operations, Part E) — so for an external company the outcome amounts to "nobody here may approve this", which is a legitimate thing for a company to configure but not the same as an escalation path. Widening the lookup would break the read-only promise, so it stays; the limitation is documented rather than quietly worked around. |
| D-227 | 2026-09-11 | A quotation that has run out gets **`expired_at`, a fact beside its status, not a new status**: a quotation that expired may still be won later, and one that was sent should still read "sent" to whoever is chasing it. `v_quotation_validity` derives `days_left` and `has_run_out` from `valid_until`, so a screen never computes a date. It also avoids adding a value to the `quotation_status` enum inside a migration, which is the kind of change that behaves differently depending on how the migration runner wraps it. |
| D-228 | 2026-09-11 | The expiry sweep is **`app.expire_quotations`, scheduled with pg_cron where pg_cron exists** — Supabase has it, the test harness does not — inside a guarded `do` block that says so in a notice rather than failing the migration. It is company-blind, `security definer`, runs with nobody signed in, writes the activity row as `actor_kind = system`, and is **revoked from `authenticated`**: it is the scheduler's, not a person's. Beside it, `check_my_quotation_expiry` does the same work for the caller's own company, for somebody who does not want to wait for the night. A quotation runs out once: running either twice changes nothing. |
| D-229 | 2026-09-11 | "Re-issue at today's prices" is **`create_costing_revision` followed by `copy_panel` for each panel** — the numbering, the family and the history from the first, the re-pricing from the second, both already tested — rather than a third function that re-prices. The revision arrives as a copy at the old prices, its panels are dropped and rebuilt the way a copy builds them, and `price_snapshot_at` is set to now. What could not be re-priced is named, exactly as in a copy. The approved revision is untouched and stays the record of what was agreed. |
